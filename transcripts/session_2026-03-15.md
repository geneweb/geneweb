# Session Transcript — 2026-03-15

## Setup

- Launched GeneWeb via `geneweb.app` with SPIES database
- App config (`~/.geneweb/config`): `BASES_DIR="$HOME/geneweb/bases"`, default port 2317
- Note: app bundle binaries are from Mar 12 — do not include changes from Mar 14 session (fanchart, ages at events, dropdown CSS)
- Development `distribution/` has latest build but symlink to `spies.gwb` must be re-created after each `make distrib`

## Work

_(session in progress)_
