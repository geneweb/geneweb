# $Id: Makefile,v 1.2 1999-09-05 04:50:10 ddr Exp $

VERSION=2.06
REDHAT=/usr/src/redhat

all:
	@echo "make install (as root)"

install:
	cp geneweb.spec $(REDHAT)/SPECS/geneweb-$(VERSION).spec
	cp geneweb-initrc.sh $(REDHAT)/SOURCES/geneweb-initrc-$(VERSION).sh
