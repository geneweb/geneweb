(executable
 (name generate_plugin_checksums)
 (modules generate_plugin_checksums)
 (libraries geneweb_plugin))

(rule
 (target plugin_checksums.ml)
 (deps
  (glob_files_rec ../../plugins/*))
 (action
  (with-stdout-to
   %{target}
   (run ./generate_plugin_checksums.exe %{project_root}/plugins))))

(generate_sites_module
 (module geneweb_sites)
 (sites geneweb))

(library
 (public_name geneweb.gwd_lib)
 (name gwd_lib)
 (libraries geneweb geneweb_http geneweb.plugin dune-site)
 (modules request geneweb_sites))

(executable
 (package geneweb)
 (name gwd)
 (public_name gwd)
 (flags -linkall)
 (libraries
  dynlink
  geneweb
  geneweb.logs
  geneweb.plugin
  geneweb_http
  gwd_lib
  logs
  logs-syslog
  logs-syslog.unix
  ptime
  fmt
  fmt.tty
  str
  unix
  uri)
 (modules base64 gwd plugin_checksums robot))
