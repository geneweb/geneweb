(executable
 (name generate_plugin_checksums)
 (modules generate_plugin_checksums)
 (libraries geneweb_plugin))

(rule
 (target plugin_checksums.ml)
 (deps
  (alias_rec %{project_root}/plugins/plugin))
 (action
  (with-stdout-to
   %{target}
   (run ./generate_plugin_checksums.exe %{project_root}/plugins))))

(generate_sites_module
 (module geneweb_sites)
 (sites geneweb))

(library
 (public_name geneweb.gwd_lib)
 (name gwd_lib)
 (wrapped true)
 (libraries geneweb geneweb.plugin wserver dune-site cmdliner)
 (modules request geneweb_sites cmd cmd_legacy robot))

(executable
 (package geneweb)
 (name gwd)
 (public_name gwd)
 (flags -linkall)
 (libraries
  dynlink
  geneweb
  geneweb.logs
  geneweb.plugin
  gwd_lib
  logs
  logs.fmt
  logs-syslog
  logs-syslog.unix
  fmt
  fmt.tty
  str
  unix
  uri
  wserver)
 (modules base64 gwd plugin_checksums))
