(library
  (name gwrepl_deps)
  (flags -linkall)
  (libraries
    stdlib
    str
    unix
    geneweb_core
    geneweb_def
    geneweb_util
    geneweb_system_util
    geneweb_gwdb
    %%%GWDB_PKG%%%
  )
  (modules)
)

(rule
  (target data.ml)
  (deps .depend (:maker mk_data.ml))
  (action (with-stdout-to %{target} (run ocaml unix.cma %{maker})))
)

(executable
 (name gwrepl)
 (public_name gwrepl)
 (link_flags -linkall -custom)
 (libraries compiler-libs.toplevel unix)
 (preprocess
  (per_module
   ((pps ppx_blob) data)
  )
 )
 (modes byte object)
 (modules gwrepl data)
)
