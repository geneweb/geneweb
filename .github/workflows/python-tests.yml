name: Python Library Tests

on:
  push:
    branches:
      - '**'
    paths:
      - 'src/python/**.py'
      - 'src/Makefile'
      - 'src/python/tests/**.py'
      - '.github/workflows/python-tests.yml'
  pull_request:
    branches:
      - '**'
    paths:
      - 'src/**.py'
      - 'src/Makefile'
      - 'src/tests/**.py'
      - '.github/workflows/python-tests.yml'

env:
  DEFAULT_BRANCH: master

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.13] # Add other versions as needed
    defaults:
      run:
        working-directory: ./src
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov
          pip install -e .
      - name: Generate test documentation
        if: (github.event_name == 'push' && startsWith(github.ref_name, env.DEFAULT_BRANCH)) || (github.event_name == 'pull_request')
        run: |
          if [ -f ../documentation/TESTING_POLICY.md ]; then
            cp ../documentation/TESTING_POLICY.md TESTS.md
          else
            echo "# Testing Policy" > TESTS.md
            echo "---" >> TESTS.md
          fi
          grep -x -- "---" TESTS.md &> /dev/null || echo "---" >> TESTS.md
          make gen_test_doc
          mv TESTS.md ../documentation/TESTING_POLICY.md
      - name: Commit and push TESTING_POLICY.md
        if: (github.event_name == 'push' && startsWith(github.ref_name, env.DEFAULT_BRANCH)) || (github.event_name == 'pull_request')
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add ../documentation/TESTING_POLICY.md -f # Force add because of .gitignore
          git commit -m 'Update TESTING_POLICY.md' || echo "No changes to commit"
          git push origin HEAD:${{ github.ref_name }} || echo "No changes to push"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # This token is automatically provided by GitHub Actions

      - name: Run tests
        run: |
          make tests-cov
