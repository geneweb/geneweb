(alias
  (name plugin)
  (deps plugin_lib_res.cmxs)
)

(rule
 (target plugin_lib_res.ml)
 (action (run touch %{target}))
)

(executable
  (name plugin_lib_res)
  (embed_in_plugin_libraries res)
  (flags -linkall)
  (modes (native plugin))
)
