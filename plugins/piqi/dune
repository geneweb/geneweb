(alias
  (name plugin)
  (deps (file META) plugin_piqi.cmxs)
)

(executable
  (name plugin_piqi)
  (embed_in_plugin_libraries piqirun piqilib piqirun.ext piqirun.pb)
  (modes (native plugin))
  (optional)
)
